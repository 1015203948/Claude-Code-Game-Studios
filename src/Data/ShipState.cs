namespace Game.Data {
    /// <summary>
    /// Ship lifecycle states. Stub — full implementation in Story 005.
    /// Values: DOCKED, IN_TRANSIT, IN_COCKPIT, IN_COMBAT, DESTROYED
    /// </summary>
    public enum ShipState {
        DOCKED,
        IN_TRANSIT,
        IN_COCKPIT,
        IN_COMBAT,
        DESTROYED
    }
}
